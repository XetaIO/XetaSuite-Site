---
title: Installation Backend
description: Guide d'installation du backend Laravel pour XetaSuite ERP.
---

import { Steps, Tabs, TabItem, Aside } from '@astrojs/starlight/components';

Ce guide vous accompagne dans l'installation du backend Laravel de XetaSuite.

## Prérequis

- **PHP** 8.4+
- **Composer** 2.x
- **PostgreSQL** 18
- **Redis** (optionnel, pour les queues)

## Installation

<Steps>

1. **Cloner le repository**

   ```bash
   git clone https://github.com/xetaio/XetaSuite-core.git
   cd XetaSuite-core
   ```

2. **Installer les dépendances PHP**

   ```bash
   composer install
   ```

3. **Configurer l'environnement**

   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

4. **Configurer la base de données**

   ```bash
   DB_CONNECTION=pgsql
   DB_HOST=127.0.0.1
   DB_PORT=5432
   DB_DATABASE=xetasuite
   DB_USERNAME=your_user
   DB_PASSWORD=your_password
   ```

5. **Exécuter les migrations**

   ```bash
   php artisan migrate --seed
   ```

6. **Lancer le serveur**

   ```bash
   php artisan serve
   ```

</Steps>

## Configuration Sanctum

<Aside type="tip">
  XetaSuite utilise **Sanctum stateful authentication** (cookies) pour l'authentification SPA.
</Aside>

Configurez les domaines autorisés dans `.env` :

```bash
SANCTUM_STATEFUL_DOMAINS=xetasuite.test
SESSION_DOMAIN=.xetasuite.test
```

## Prochaines étapes

- [Installation Frontend](/getting-started/frontend/) - Configurez l'application React
- [Configuration](/getting-started/configuration/) - Options de configuration avancées
